server {
    listen 80;
    listen [::]:80;
    server_name www.jidehub.top;

    location ^~ /.well-known/acme-challenge/ {
        root /var/www/letsencrypt;
    }

    location / {
        root /var/www/jidehub.top/current;
        index index.html;
        try_files $uri $uri/ =404;
    }
}
